coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%
        only_pulls: true
    patch:
      default:
        target: auto
        threshold: 0%
        only_pulls: true
